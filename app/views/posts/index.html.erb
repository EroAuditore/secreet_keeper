
<section class="section">
  <div class="container">
    <div class="columns">
      <div class="column is-one-quarter">
        <%= render 'top' %>
      </div>
      <div class="column is-half">
        <%= render partial: 'form' %>

        <%= render partial: 'feed' %>
      </div>
      <div class="column is-one-quarter">
      <% if user_signed_in? %>
         <%= render partial: 'authors' %>
      <% end %>
      </div>
    </div>
  </div>
</section>

